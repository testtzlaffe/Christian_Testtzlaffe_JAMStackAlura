import styled from 'styled-components';

const WrapperCards = styled.div`
  height: 500px;
  width: 500px;
  background-color: green;
`;

export const Cards = () => {
  return <WrapperCards>Cards</WrapperCards>;
};
